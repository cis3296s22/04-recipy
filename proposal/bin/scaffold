#!/usr/bin/env python3

import sys
import os

try:
    from scaffold import cli
    cli()
except KeyboardInterrupt:
    print()
    try:
        sys.exit(0)
    except SystemExit:
            os._exit(0)

